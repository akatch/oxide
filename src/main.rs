extern crate dicom_core;

mod c_echo;
mod association;

fn main() {
    //let filename = String::from("test.dcm");
    //print_file(&filename);

    //println!("{:?}", c_echo::build_rq());

    /*
     * listen on some port
     * accept associations
     * do stuff with the associations, like:
     * - respond to echo
     * - store a study
     * - perform a query
     *
     */
}
